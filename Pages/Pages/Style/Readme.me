# Gauteng Loadboard (Next.js + Supabase + SerpAPI)

## Setup
1. Create Supabase project and run `schema.sql` in SQL editor to create `loads` table.
2. Copy `.env.local.example` to `.env.local` and add your keys.
3. Install dependencies:
   ```bash
   npm install
